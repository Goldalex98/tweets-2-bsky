---
description: Sync schema/identity docs after config changes
---

Follow `.cursor/skills/sync-schema-docs/SKILL.md`.

Update only docs that drifted from the current code (schema version, identity model, APIs, encryption paths, troubleshooting). Keep examples redacted.
