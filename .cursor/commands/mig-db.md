---
description: Add the next additive SQLite migration
---

Follow `.cursor/skills/add-sqlite-migration/SKILL.md`.

Create the next numbered migration under `src/db/migrations/`, register it, and extend `tests/integration/database-migrations.test.ts` using a temporary data directory.
